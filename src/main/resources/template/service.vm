package ${ServicePackage};

import com.github.pagehelper.PageInfo;
import ${ModelName};

import java.util.List;

/**
 * @description: 服务层接口
 */
public interface ${ServiceSimpleName} {

    /**
     * 添加
     *
     * @param record
     */
    int add(${ModelSimpleName} record);

    /**
     * 修改
     *
     * @param record
     */
    int update(${ModelSimpleName} record);

    /**
     * 真实删除
     *
     * @param id
     * @return
     */
    int delete(String id);

    /**
     * 根据Id查询
     *
     * @param id
     * @return
     */
    ${ModelSimpleName} selectById(String id);

    /**
    * 分页查询
    *
    * @param record
    * @return
    */
    PageInfo<${ModelSimpleName}> selectByPage(${ModelSimpleName} record);

    /**
    * 全量查询列表
    *
    * @param record
    * @return
    */
    List<${ModelSimpleName}> selectList(${ModelSimpleName} record);

}
